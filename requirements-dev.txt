# Dependencies for the local dev docker-compose stack (docker-compose.dev.yml)
# AND for running the test suite (backend/tests/, incl. .github/workflows/test.yml).
# Everything backend/ and frontend_fastapi/ need, per the root requirements.txt,
# plus frontend/ (Django) -- which has no dependency file of its own anywhere
# in the repo today. Versions pinned to what's already validated working in
# this repo's own .venv.
-r requirements.txt

Django==6.0.7
django-cotton==2.7.2

# Test suite (backend/tests/, frontend_fastapi/tests/) -- installed but
# previously undeclared anywhere in the repo, so CI would otherwise pull in
# whatever's latest/absent rather than a validated version.
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1  # httpx is already in requirements.txt for app use; pinned
               # explicitly here too since fastapi.testclient.TestClient
               # depends on it and a future app-side bump shouldn't
               # silently also change what the test suite runs against

# PinnacleExport's own dependencies (regex, tqdm, pandas, pymedphys, ...)
# are deliberately NOT listed here -- installed as a separate CI step
# (.github/workflows/test.yml, right BEFORE this file, so this file's own
# pins like pytest above still win) from PinnacleExport's own
# requirements.txt when the submodule is actually checked out. See that
# step's comment for why (and for the Python-version story this depends on).
#
# joblib is the one exception: PinnacleExport's src/export.py imports it
# directly (`from joblib import Parallel, delayed`), but it's genuinely
# absent from PinnacleExport's own requirements.txt and isn't pulled in
# transitively by anything else that file lists either -- confirmed by
# statically scanning every import in that submodule's source tree against
# what's actually installed after both requirements files above. A real
# gap in a separate, private repo this project doesn't control, patched
# here rather than left to break CI collection again.
joblib==1.5.2
