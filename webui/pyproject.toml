[project]
name = "hermes-webui-test"
version = "0.1.0"
description = "Minimal Django frontend for testing HERMES backend functionality (not production-ready)"
requires-python = ">=3.11"
dependencies = [
    "django",
    "requests",
    "python-dotenv",
]
