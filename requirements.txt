# HERMES — Python dependencies
# Covers both the backend (FastAPI) and gateway (FastAPI + Streamlit) packages.

# Web framework / servers
fastapi
uvicorn[standard]
python-multipart  # required by FastAPI's File()/Form() (retrieve + export CSV upload endpoints)

# HTTP clients
httpx
requests

# Streamlit UI
streamlit

# Data handling
pandas
polars
numpy

# DICOM
pydicom
pynetdicom
pyorthanc

# ProKnow cloud RT platform
proknow

# PostgreSQL (HermesDB job/event tracking, anonymisation-mapping lookups)
psycopg2-binary
alembic
sqlalchemy

# Utilities
python-dotenv
pydantic

# Testing
pytest
