# HERMES — Python dependencies
# Covers the backend (FastAPI), the new frontend_fastapi/ frontend, and the
# Streamlit-era pages still present pending their own cleanup.

# Web framework / servers
fastapi
uvicorn[standard]
python-multipart  # UploadFile support (backend/ import-CSV uploads, frontend_fastapi/ forms)

# Templating (frontend_fastapi/)
Jinja2

# HTTP clients
httpx
requests
sseclient

# Streamlit UI
streamlit

# Data handling
pandas
polars
numpy

# DICOM
pydicom
pynetdicom
pyorthanc

# ProKnow cloud RT platform
proknow

# PostgreSQL (anonymisation DB)
psycopg2-binary

# Database migrations (backend/'s HermesDB, and frontend_fastapi/'s own local DB)
alembic
SQLAlchemy

# frontend_fastapi/ — auth, sessions, CSRF, forms
argon2-cffi       # password hashing
itsdangerous      # signed invite/activate tokens
WTForms           # form field validation (Jinja2 templates render fields directly)
email-validator   # WTForms' Email() validator requires this explicitly
# CSRF is hand-rolled (frontend_fastapi/deps.py: csrf_protect), tied to the
# session-stored token rather than a separate stateless cookie -- no
# extra dependency needed for it.

# Utilities
python-dotenv
pydantic
