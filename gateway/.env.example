# URL of the Hermes backend on the secure internal network (used by FastAPI gateway)
HERMES_URL=http://192.168.x.x:8000

# Gateway FastAPI address (used by the Streamlit frontend to call the gateway API)
GATEWAY_URI=localhost
GATEWAY_PORT=8001

# Optional: set to DEBUG for verbose logging
LOG_LEVEL=INFO

# ── Anonymisation DB (required in production) ─────────────────────────────────
# PostgreSQL database mapping anonymised patient IDs to real patient IDs.
# Must be reachable from the gateway host. See gateway/anon.py for SQL queries.
# If ANON_DB_HOST is not set, anonymisation is skipped (passthrough mode — dev only).
# ANON_DB_HOST=localhost
# ANON_DB_PORT=5432
# ANON_DB_NAME=anon_mapping
# ANON_DB_USER=gateway
# ANON_DB_PASS=

# ── Remote PACS (optional — enables PACS comparison features) ─────────────────
# The PACS must be reachable from the gateway host's network.
# PACS_AE_TITLE=PACS
# PACS_HOST=192.168.x.x
# PACS_PORT=104
# GATEWAY_AE_TITLE=HERMES_GW   # AE title the gateway presents when initiating DICOM connections
